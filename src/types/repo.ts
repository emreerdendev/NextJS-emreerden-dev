/** A repository mapped to the shape the portfolio UI renders. */
export interface Repo {
  url: string
  live: string
  name: string
  desc: string
  stars: number
  topics: string[]
  img: string
}
